# Spectral Ruleset
# Generated from: Microsoft Graph API Guidelines
# Version: 1.0.0
#
# This file was auto-generated from an api-style-spec profile.
# Do not edit manually - modify the source profile instead.

extends:
  - spectral:oas

rules:

  # GRAPH-URI-003: Use v1.0 for production, beta for preview
  # v1.0 is the stable production version; beta is for preview features that may ...
  graph-uri-003:
    description: "Use v1.0 for production, beta for preview"
    message: "v1.0 is the stable production version; beta is for preview features that may change."
    severity: error
    given: "$.servers[*].url"
    then:
      function: pattern
      functionOptions:
        match: "/(v1\\.0|beta)/"

  # GRAPH-NAME-001: Use camelCase for property names
  # Consistent naming improves API usability.
  graph-name-001:
    description: "Use camelCase for property names"
    message: "Consistent naming improves API usability."
    severity: error
    given: "$.components.schemas[*].properties[*]~"
    then:
      function: casing
      functionOptions:
        type: camel
